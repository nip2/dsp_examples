# Impulse detection in a noisy signal

To view, open main.ipynb

Given waveform data in a .wav format, this script uses frequency analysis to filter out noise in order to detect when EKG type spikes occur.
